"use client";

const MemberCardBtn = () => {
	return (
		<button
			type="button"
			className="inline-flex items-center justify-center rounded-full bg-white px-4 py-1 text-sm font-medium text-black shadow-sm"
		>
			我的
		</button>
	);
};

export default MemberCardBtn;
